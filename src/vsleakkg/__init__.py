"""VS-LeakKG v2 namespace.

The v2 framework lives under `vsleakkg.v2`. This top-level package exists only
to host the `v2` sub-package; there is no v1 code in this repository (see the
companion v1 repo at https://github.com/kongwoang/VS-LeakKG).
"""

__version__ = "2.0.0-dev"
